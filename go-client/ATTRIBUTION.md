# Attribution（go-client 純玩版）

本目錄／`static/` 內用於 go shell 的美術／音效／字型署名。授權不要求署名時，本專案仍署名。

---

## 遊樂場大廳 Lobby（`static/lobby/`）

大廳地圖 tiles 由作者提供的 **Google Gemini** 模組化貼圖集裁切（`source-tileset.png`）。切片：`scripts/slice-go-lobby-tileset.py`（原點 `(0, 101)`；依偵測格線切不規則格子，不含 1px 格線；米色底去背）。物件尚未組裝。

| 來源 | 素材 | 備註 |
| --- | --- | --- |
| Google Gemini（作者提供） | 室內遊樂場大廳 tileset | 見 `static/lobby/SOURCE.txt` |
